import React from 'react';
import './style.css';

const logo=(props)=>{
    return(
        <div className="logo">
             <a href="#">sri mallikarjuna swami silks</a>
             <div className='tag'>garments & immitation jewellers</div>
             <div className="stock">dress materials || sarees || matching centre || kids wear || inner wears || fancy bangles || bindi || rubber bands || hair clips || mehandi cones || fancy goods</div>
        </div>
    )
}

export default logo;